function setup() {
  createCanvas(1920, 960);
}

function draw() {
  background(220);
}

class Ship {
  constructor(lives){
    this.lives = lives;
  }
  /*if lives == 0 *die* */

}

class bunker {
  constructor(lives){
    this.lives = lives;
  }
  /*if lives == 0 *die* */
}

class alien {
  constructor(lives){
    this.lives = lives;
  }
  /*if lives == 0 *die* */
}